/**
 * Single-page resume site (continuous scroll). GitHub Pages `basename` kept for future routes if needed.
 */
import { HomePage } from './pages/HomePage'

function App() {
  return <HomePage />
}

export default App
